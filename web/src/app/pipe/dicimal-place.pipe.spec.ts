import { DicimalPlacePipe } from './dicimal-place.pipe';

describe('DicimalPlacePipe', () => {
  it('create an instance', () => {
    const pipe = new DicimalPlacePipe();
    expect(pipe).toBeTruthy();
  });
});
